#include <stdio.h>

int main() {
int n;
int i;
int sum;
printf("Please enter a positive number: ");
scanf("%d", &n);
for(i = 1; i <= n; ++i) {
    sum = (i); //needs fixing.
    }
printf("%d", sum);
    return 0;
}